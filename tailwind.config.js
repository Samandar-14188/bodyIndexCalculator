/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./**/*.html", "./**/*.js"], // Matches all HTML and JS files in any folder
  theme: {
    extend: {},
  },
  plugins: [],
}
